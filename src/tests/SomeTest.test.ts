import ViewModeValue from '../store/reducers/ViewModeValue';

describe('ViewModeValue', () => {
  it('should be defined', () => {
    expect(ViewModeValue).toBeDefined();
  });
  it('should be a function', () => {
    expect(typeof ViewModeValue).toBe('function');
  });
});
