const NotFoundPage = () => {
  return (
    <div className="notFoundPage" style={{ color: 'white', padding: '30px' }}>
      Page not found. Maybe you write wrong URL?
    </div>
  );
};

export default NotFoundPage;
